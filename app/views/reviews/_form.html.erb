<%= simple_form_for([restaurant, review],
    data: { insert_in_list_target: "form", action: "submit->insert-in-list#send"}) do |f| %>
  <%= f.input :content, input_html: { data: { insert_in_list_target: "input" }} %>
  <%= f.button :submit, class: "btn btn-primary" %>
<% end %>
